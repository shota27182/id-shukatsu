<div class="profile-container profile-top">
    <a href="/mypage" class="profile-mypage-link"><マイページに戻る</a>
    <h2>お知らせ</h2>
    
    <div class="profile-form">
        <div class="profile-companies">
            <% @notifications.order(id: :desc).each do |i| %>
            <div class="profile-notification-index">
                <p><%= i.created_at.strftime("%Y/%m/%d") %></p>
                <a href='/mypage/notifications/<%= i.id %>'><%= i.title %></a>
            </div>
            <% end %>
        </div>
    </div>
</div>
<div class="profile-container">
    <a href="/mypage" class="green-button"><  マイページに戻る</a>
</div>
