<header>
    <div class="container">
        <div class="header-left">
            <%=link_to image_tag("logo.svg", class:"logo"), root_path %>
        </div>
        <div class="header-message">
            <p>楽しく自由に就職活動する時代へ</p>
        </div>
        <div class="header-right">
            <% if logged_in %>
              <%=link_to image_tag('alart.svg'), mypage_notification_path,{class:"header-image-right"} %>
              <%=link_to image_tag('mypage.svg'), mypage_path  %>
            <% else %>
              <a href="/login" class="btn login">ログイン</a>
              <a href="/users/sign_up" class="btn signup">会員登録</a>
            <% end %>
        </div>
    </div>
</header>