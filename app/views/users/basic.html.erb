<div class="sign-up">
    <div class="form-heading">プロフィール登録(ステップ1/2)</div>
    <div class="form-explaration">
        <p>ID就活ではご登録情報を元に最適な就活サービスを提供いたします。<br>プロフィール情報は必ずご入力いただきますようお願いいたします。</p>
        <p class="profile-promise">※学生にとって不利益になる情報を企業にリークしたり、公開することは一切ありません。詳しくはプライバシーポリシーをご覧ください。</p>
    </div>
    <%= form_for(@user, url:users_basics_path, html: {method: "put"}) do |f| %>
      <div class="general-profile">
        <%= f.hidden_field :name, value:@user.name %>
        <%= f.hidden_field :email, value:@user.email %>
        <%= f.hidden_field :id, value:@user.id %>
        <div class="general-title">基本情報</div>
        <%= f.fields_for :general do |af|  %>
          <div class="form-detail form-top">
              <p>名前</p>
              <div class="form-field">
                <p><%= @user.name %></p>
              </div>
          </div>
          <div class="form-detail">
              <p>フリガナ</p>
              <%=af.text_field :kana ,class:"form-field"%>
          </div>
          <div class="form-detail">
              <p>性別</p>
              <div class="profile-radio">
                 <%= af.radio_button :sex, :male, class:"profile-radio-button" %>
            　    <%=af.label "男性", class:"profile-radio-label gender-left" %>
            　    <%= af.radio_button :sex, :female, class:"profile-radio-button" %>
            　    <%= af.label "女性", class:"profile-radio-label gender-right"%>
            　</div>
          </div>
          <div class="form-detail">
              <p>住所１</p>
              <%=af.text_field :address1, class:"form-field" %>
          </div>
          <div class="form-detail">
              <p>住所２</p>
              <%=af.text_field :address2, class:"form-field" %>
          </div>
          <div class="form-detail">
              <p>建物名・部屋番号</p>
              <%=af.text_field :building_name, class:"form-field" %>
          </div>
          <div class="form-detail">
             <p>電話番号</p>
             <%= af.text_field :tel,placeholder: '08012341234', required: true ,class:"form-field"%>
          </div>
        <% end %>
      </div>
      <div class="general-profile">
        <div class="general-title">所属情報</div>
        <%= f.fields_for :education do |bf| %>
        　<div class="form-detail profile-birthday">
              <p>出身高校</p>
              <%=bf.text_field :highschool, class:"form-field" %>
          </div>
        <% end %>
      </div>
        <%= f.submit "次のステップへ進む", class: "form-submit" %>
    <% end %>
</div>