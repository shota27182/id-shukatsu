<% user_languages.each do |user_language| %>
    <p class="eiken-right eiken-dark"><%= user_language.language %></p>
    <td class="eiken-left"><%= link_to '編集', edit_user_language_path(user_language), remote: true %>・<%= link_to '削除',user_language_path(user_language), method: :delete, remote: true %></td>
<% end %>