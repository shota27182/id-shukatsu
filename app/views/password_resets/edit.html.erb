<% provide(:title, '新規登録') %>
<div class="sign-up">
    <div class="form-heading">パスワードの再設定</div>
    <div class="form-explaration">
        <p>ID就活に会員登録すると、全ての選考対策コンテンツが無料で見放題！</p>
        <p>会員限定の特別イベントや選考のご招待をお届けします</p>
    </div>
    
  <%= form_for(@user, url: password_reset_path(params[:id])) do |f| %>
          <div class="form-detail">
            <p>パスワード</p>
            <%=f.password_field :password, class: "form-field"  %>
            <% if @user.errors.include?(:password) %>
            <p style="color: red;"><%= @user.errors.full_messages_for(:password).first %>
            <% end %>
          </div>
          <div class="form-detail">
            <p>もう一度パスワードを入力してください</p>
            <%= f.password_field :password_confirmation, class: "form-field" %>
            <% if @user.errors.include?(:password_confirmation) %>
            <p style="color: red;"><%= @user.errors.full_messages_for(:password_confirmation).first %>
            <% end %>
          </div>
          <%=f.submit "パスワードを再設定する", class: "form-submit" %>
  <% end %>
  <div class="form-confirmation">ログインすることにより、当社の<a href="#" class="">利用規約</a>および<a href="#" class="">ライバシーポリシー規約</a>に同意したとみなされます。</div>
  <div class="mail-guide"><a href="#">>確認メールが届いていない方はこちら</a></div>
  <div class="login-guide"><a href="#" >>ログインはこちら</a></div>
</div>
